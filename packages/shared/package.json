{
    "name":  "@core/shared",
    "version":  "0.1.0",
    "private":  true,
    "type":  "module",
    "main":  "src/index.ts",
    "types":  "src/index.ts",
    "scripts":  {
                    "build":  "tsc -p tsconfig.json",
                    "dev":  "tsc -w -p tsconfig.json",
                    "lint":  "echo \"No lint configured for @core/shared\"",
                    "typecheck":  "tsc -p tsconfig.json --noEmit",
                    "format":  "prettier --check \"src/**/*.ts\""
                },
    "dependencies":  {
                         "zod":  "^3.24.2"
                     }
}
